# Requirements needed when generating releases. See BUILD.rst for details.
# To install each of them: pip install -r requirements.txt
invoke >= 0.20
rellu >= 0.6
docutils >= 0.14
robotframework >= 3.1.1
robotframework-seleniumlibrary >= 3.3.1
pyyaml >= 6.0.2