# Requirements needed when generating releases. See BUILD.rst for details.
# To install each of them: pip install -r requirements.txt
invoke >= 0.20
rellu >= 0.6
docutils >= 0.14
robotframework >= 3.1.1
robotframework-browser >= 18.9.1
robotframework-openapidriver >= 4.3.0
robotframework-tidy >= 4.14.0
pyyaml >= 6.0.2
python-dotenv >= 1.0.1